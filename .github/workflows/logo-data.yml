name: Generated files in sync

on:
  push:
  pull_request:

jobs:
  logo-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check docs/assets/logo-data.js is up to date
        run: python tools/build_logo_data.py --check